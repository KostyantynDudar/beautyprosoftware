# 📌 Оновлений README для проєкту

## 🔹 Опис проєкту
Цей проєкт автоматизує отримання послуг з API, оновлення цін та керування основним файлом послуг. Скрипти працюють за розкладом (cron) та включають логування й повідомлення у Telegram.

## 📁 Структура проєкту
- `update_token.py` – отримує та оновлює токен доступу до API.
- `fetch_and_update.py` – отримує всі послуги одним запитом, аналізує зміни та оновлює основний файл.
- `start_all.py` – керує запуском всіх процесів та відправляє результати у Telegram.

## 🔧 Встановлення та запуск
1. Завантажте репозиторій.
2. Встановіть необхідні залежності:  
   ```bash
   pip install -r requirements.txt
   ```
3. Запустіть `start_all.py` вручну або налаштуйте виконання через `cron`.

## 📊 Логування
Всі події записуються у файл:
- `/home/veronicaplus/public_html/wp-content/uploads/scripts/fetch_update_log.txt`

## 📩 Повідомлення у Telegram
Після оновлення послуг у Telegram надходить статистика:
- 🔄 Кількість оновлених цін
- ➕ Додані нові послуги
- ❌ Видалені послуги
- 📝 Загальний підсумок змін

Цей механізм дозволяє швидко отримувати звітність без потреби перевіряти логи вручну.

## 🕒 Автоматизація через Cron
У `crontab` налаштоване щоденне виконання скрипта `start_all.py`:
```bash
40 7 * * * cd /home/veronicaplus/public_html/wp-content/uploads/scripts && /usr/bin/python3 start_all.py > start_all_log.txt 2>&1
```
Це забезпечує регулярне оновлення даних без ручного втручання.

